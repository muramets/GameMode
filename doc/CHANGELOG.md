# Changelog

История изменений и улучшений в RPG Therapy.

## 🏗️ Недавние изменения (Декабрь 2024)

### ✅ Исправления и улучшения

#### 🚨 КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Баг Quick Actions (30.05.2025)
- **Проблема**: Quick Actions не добавлялись при нажатии кнопки добавления
  - Показывался toast об успешном добавлении, но элемент не появлялся
  - Отсутствовало логирование операций в истории
  - Рассинхронизация между `QUICK_ACTIONS` и `QUICK_ACTION_ORDER` массивами

- **Корневая причина**: 
  - `addToQuickActions()` обновлял только `QUICK_ACTIONS`, но не `QUICK_ACTION_ORDER`
  - UI рендеринг использует `getQuickActionsInOrder()` вместо `getQuickActions()`
  - Отсутствовало логирование операций добавления/удаления

- **Исправления**:
  - ✅ **storage.js**: Исправлен `addToQuickActions()` - теперь обновляет оба массива
  - ✅ **storage.js**: Исправлен `removeFromQuickActions()` - добавлено логирование
  - ✅ **ui.js**: Добавлена поддержка рендеринга `quick_action` записей в истории  
  - ✅ **app.js**: Добавлена поддержка поиска по `quick_action` записям

- **Результат**: Quick Actions теперь добавляются корректно с полным логированием

#### 🔧 Исправление заголовков таблицы протоколов (30.05.2025) ✅ РЕШЕНО
- **Проблема**: Заголовки таблицы протоколов смещены относительно данных
  - Заголовок "weight" отображается над столбцом с чекинами
  - Заголовок "actions" переносится на следующую строку  
  - Столбец с весом не имеет заголовка

- **Корневая причина**: CSS конфликт селекторов
  - В `css/modals.css` общий селектор `.protocols-header` переопределял настройки основной таблицы
  - Modal задавал 4 столбца: `60px 300px 200px 80px`
  - Основная таблица требовала 5 столбцов: `60px 300px 200px 80px 100px`
  - Конфликт специфичности CSS приводил к неправильному grid layout

- **Диагностика**: 
  - Добавлены временные debug borders для визуализации grid-колонок
  - Обнаружено: 6 контейнеров, 5 заголовков HTML, но 4 grid-колонки в CSS
  - Выявлен конфликт между таблицами основной страницы и модального окна

- **Решение**: 
  - Изменен общий селектор `.protocols-header` на специфичный `.quick-action-protocols-table .protocols-header` в `css/modals.css`
  - Это изолировало стили модального окна от основной таблицы
  - Удалены временные debug borders

- **Результат**: ✅ Все заголовки теперь правильно выровнены со столбцами данных

- **Файлы**: `css/modals.css` (исправление селектора)

#### 1. Удаление hover эффектов в таблицах
- **Проблема**: Нежелательные цветовые изменения при наведении на строки таблиц
- **Решение**: Удалены CSS правила `.skill-name-cell:hover` и `.history-row:hover`
- **Файлы**: `css/tables.css`

#### 2. Консистентность заголовков таблиц
- **Обновление**: Проверена унификация шрифтов, размеров и цветов заголовков
- **Статус**: Все заголовки (history, skills, protocols) используют идентичную стилизацию
- **Файлы**: `css/tables.css`

#### 3. Реструктуризация Quick Actions Layout
- **Изменение**: Переход с CSS Grid на Flexbox
- **Преимущества**:
  - Автоматический перенос элементов на новые строки
  - Максимум 5 элементов в ряду
  - Лучшая адаптивность для мобильных устройств
- **CSS**: 
  ```css
  .quick-protocols {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
  }
  
  .quick-protocol {
    flex: 0 0 calc(20% - 0.6rem); /* 5 элементов в ряду */
  }
  ```
- **Файлы**: `css/dashboard.css`

#### 4. Удаление лимита Quick Actions ⭐
- **Критическое изменение**: Убрано искусственное ограничение до 5 элементов
- **Исправления**:
  - Удален лимит в `addToQuickActions()` методе
  - Удален лимит в `setQuickActions()` методе
  - Обновлены комментарии в коде
- **Файлы**: `js/storage.js`

#### 5. Исправление удаления Quick Actions 🐛
- **Проблема**: Кнопка удаления (trash) перестала работать после предыдущего исправления
- **Причина**: `removeFromQuickActions()` не обновлял массив `QUICK_ACTION_ORDER`
- **Решение**: Добавлено удаление элемента из обоих массивов
- **Код**:
  ```javascript
  removeFromQuickActions(protocolId) {
    // Удаляем из quick actions
    const updatedQuickActions = quickActions.filter(id => id !== protocolId);
    
    // ИСПРАВЛЕНИЕ: Также удаляем из order
    const quickActionOrder = this.getQuickActionOrder();
    const updatedOrder = quickActionOrder.filter(id => id !== protocolId);
    this.set(this.KEYS.QUICK_ACTION_ORDER, updatedOrder);
    
    this.set(this.KEYS.QUICK_ACTIONS, updatedQuickActions);
  }
  ```
- **Файлы**: `js/storage.js`

#### 6. Восстановление кнопки check-in в таблице протоколов
- **Проблема**: Отсутствовала кнопка "check-in" в таблице протоколов
- **Решение**: Добавлена 5-я колонка "action" с кнопкой check-in
- **Изменения**:
  - Обновлен HTML заголовка таблицы протоколов
  - Изменен CSS grid с 4 на 5 колонок: `60px 300px 1fr 80px 100px`
  - Добавлены стили для `.protocol-checkin-btn`
  - Кнопка появляется при hover на строку протокола
  - Модифицирован `renderProtocols()` в `ui.js`
- **CSS**:
  ```css
  .protocol-checkin-btn {
    opacity: 0;
    visibility: hidden;
    transition: all 0.125s;
  }
  
  .protocol-row:hover .protocol-checkin-btn {
    opacity: 1;
    visibility: visible;
  }
  ```
- **Файлы**: `index.html`, `css/tables.css`, `js/ui.js`

### 🎨 Drag & Drop улучшения

#### Исправление визуальных артефактов
- **Проблема**: Некорректные размеры drag images для разных типов элементов
- **Решения**:
  - Ограничение ширины для table rows (максимум 400px)
  - Точное копирование CSS свойств для states/quick actions
  - Удаление border/glow эффектов по запросу пользователя
- **Файлы**: `js/dragdrop.js`

### 📱 Responsive дизайн

#### Адаптивность Quick Actions
- **Mobile (≤768px)**: 2 элемента в ряду
- **Ultra Mobile (≤480px)**: 1 элемент в ряду
- **Desktop**: 5 элементов в ряду

### 💾 Персистентность данных

#### Подтверждение работы localStorage
- **Статус**: Все операции используют localStorage как основную базу данных
- **Автосохранение**: Все изменения сохраняются мгновенно
- **Методы Storage**: Обеспечивают немедленную персистентность

## 🏃‍♂️ Оптимизация производительности

### Минимизация DOM операций
- Efficient event delegation для динамических элементов
- Batch операции при множественных изменениях
- Кеширование часто используемых элементов

### CSS оптимизации
- Использование CSS переменных для согласованности
- Transition только для необходимых свойств
- Оптимизированные селекторы

## 🧪 Тестирование и QA

### Проверенные сценарии
- ✅ Добавление неограниченного количества Quick Actions
- ✅ Удаление Quick Actions через trash кнопку
- ✅ Drag & drop в разных секциях
- ✅ Responsive поведение на всех устройствах
- ✅ Check-in протоколов из таблицы
- ✅ Сохранение данных при перезагрузке

### Устраненные баги
- 🐛 Quick Actions deletion не работало
- 🐛 Лимит 5 элементов был слишком ограничивающим
- 🐛 Отсутствующая кнопка check-in в протоколах
- 🐛 Hover эффекты в неподходящих местах
- 🐛 Некорректные drag images

## 🔮 Планы на будущее

### В разработке
- [ ] Статистика использования Quick Actions
- [ ] Группировка протоколов по категориям
- [ ] Backup/restore функциональность
- [ ] Cloud synchronization

### Рассматриваемые улучшения
- [ ] Keyboard shortcuts для Quick Actions
- [ ] Пользовательские иконки протоколов
- [ ] Анимации при добавлении/удалении
- [ ] Multi-select operations

## 📊 Статистика изменений

### Файлы затронуты в последнем обновлении
- `js/storage.js` - исправления методов Quick Actions
- `css/dashboard.css` - flex layout для Quick Actions
- `css/tables.css` - удаление hover эффектов, добавление check-in кнопки
- `js/ui.js` - обновление renderProtocols()
- `index.html` - добавление 5-й колонки в таблицу протоколов
- `js/dragdrop.js` - улучшения drag images

### Строки кода
- **Добавлено**: ~50 строк CSS, ~30 строк JavaScript
- **Удалено**: ~15 строк CSS (hover эффекты)
- **Изменено**: ~25 строк JavaScript (методы Storage)

## [2025-05-30] - Bug Fixes & Documentation

### Fixed
- **Protocols Table Header Misalignment** - Заголовки таблицы протоколов больше не смещены
  - **Проблема**: Столбец "targets" использовал `1fr` в CSS Grid, из-за чего растягивался и смещал заголовки "weight" и "actions"
  - **Решение**: Заменил `1fr` на фиксированную ширину `200px` для столбца "targets" 
  - **Файлы**: `css/tables.css`, `css/modals.css`, `css/responsive.css`
  - **Результат**: Все заголовки теперь правильно выровнены с соответствующими столбцами данных

- **Quick Actions Not Adding Items** - Исправлена ошибка, когда Quick Actions показывали toast об успехе, но элементы не добавлялись
  - **Проблема**: `addToQuickActions()` обновлял только `QUICK_ACTIONS` массив, но не `QUICK_ACTION_ORDER`
  - **Решение**: Исправлен `addToQuickActions()` для обновления обоих массивов + добавлено логирование
  - **Файлы**: `js/storage.js`, `js/ui.js`, `js/app.js`
  - **Дополнительно**: Добавлена поддержка отображения записей типа `quick_action` в истории

---

*Все изменения направлены на улучшение пользовательского опыта и стабильности приложения RPG Therapy.* 
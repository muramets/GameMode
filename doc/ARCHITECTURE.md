# RPG Therapy - Архитектура приложения

## Общее описание

RPG Therapy - это веб-приложение для геймификации терапии, которое превращает повседневные ритуалы и привычки в RPG-систему с навыками, протоколами и состояниями. Приложение полностью работает в браузере без серверной части, используя localStorage для хранения данных.

## Концепция системы

### Основные сущности

1. **Skills (Навыки)** - 9 основных способностей персонажа:
   - Каждый навык имеет иконку, название, описание
   - У навыка есть начальный балл (0-10) и текущий балл (рассчитывается динамически)
   - Навыки можно переупорядочивать drag & drop

2. **Protocols (Протоколы)** - ежедневные ритуалы/действия:
   - Имеют название, иконку, описание при наведении
   - Указывают действие (+ или -) и вес (0-1)
   - Связаны с 1-3 целевыми навыками
   - При выполнении изменяют баллы связанных навыков

3. **States (Состояния)** - группы навыков, показывающие общий прогресс:
   - Рассчитываются как среднее значение связанных навыков
   - Например: "Физическое здоровье" = среднее из "Сила", "Выносливость", "Ловкость"

4. **Checkins (Отметки)** - история выполненных протоколов:
   - Хранят информацию о том, когда и какой протокол был выполнен
   - Содержат изменения навыков для возможности отката

## Архитектура файлов

### HTML (index.html)
- Основная структура приложения
- Навигация в стиле Monkeytype (горизонтальные табы)
- Модальные окна для редактирования навыков и протоколов
- Поиск и фильтрация по всем разделам

### CSS (css/style.css)
- Темная тема в стиле Monkeytype
- Адаптивный дизайн
- Анимации и переходы
- Стили для drag & drop
- Модальные окна и формы

### JavaScript модули

#### 1. app.js - Главный контроллер
**Ответственность:**
- Инициализация приложения
- Управление переключением между разделами (skills/protocols/states/history)
- Поиск и фильтрация контента
- Управление тостами (уведомления)
- Настройка подсказок (tooltips)

**Ключевые переменные:**
- `currentPage` - текущий активный раздел
- `filteredSkills/filteredProtocols` - отфильтрованные данные для отображения

**Ключевые методы:**
- `init()` - инициализация всего приложения
- `navigateTo(page)` - переключение между разделами
- `handleSearch()` - обработка поиска

#### 2. storage.js - Управление данными
**Ответственность:**
- Все операции с localStorage
- CRUD операции для навыков, протоколов, состояний
- Расчет текущих баллов навыков на основе истории
- Управление порядком элементов (для drag & drop)

**Ключевые структуры данных:**
```javascript
// Навык
{
  id: уникальный_идентификатор,
  name: "Название. Описание",
  icon: "🎯",
  hover: "Подсказка при наведении",
  initialScore: 5.0
}

// Протокол
{
  id: уникальный_идентификатор,
  name: "Название протокола",
  icon: "💪",
  hover: "Описание",
  action: "+", // или "-"
  weight: 0.5, // 0-1
  targets: [id1, id2, id3] // массив ID навыков
}

// Отметка
{
  id: timestamp,
  type: "protocol",
  protocolId: id,
  timestamp: ISO_string,
  changes: { skillId: изменение }
}
```

**Ключевые методы:**
- `getSkills/getProtocols/getStates()` - получение данных
- `addSkill/addProtocol()` - добавление новых элементов
- `calculateCurrentScore(skillId)` - расчет текущего балла навыка
- `addCheckin(protocolId)` - выполнение протокола

#### 3. ui.js - Отрисовка интерфейса
**Ответственность:**
- Генерация HTML для отображения навыков, протоколов, состояний
- Обновление счетчиков и прогресс-баров
- Управление видимостью элементов

**Ключевые методы:**
- `renderSkills()` - отрисовка списка навыков
- `renderProtocols()` - отрисовка списка протоколов
- `renderStates()` - отрисовка состояний
- `renderHistory()` - отрисовка истории действий

#### 4. modals.js - Модальные окна
**Ответственность:**
- Управление модальными окнами для добавления/редактирования
- Обработка форм навыков и протоколов
- Система выбора целевых навыков для протоколов

**Особенности системы выбора навыков:**
- До 3 целевых навыков на протокол
- Прогрессивное отображение слотов (следующий появляется после заполнения предыдущего)
- Поиск с живыми предложениями
- Красивые badge-элементы для выбранных навыков
- Автоматическое смещение при удалении (без пустых слотов)

**Структура данных выбора:**
```javascript
selectedTargets: [null, null, null] // 3 слота для навыков
```

#### 5. dragdrop.js - Перетаскивание элементов
**Ответственность:**
- Реализация drag & drop для навыков и протоколов
- Сохранение нового порядка в storage
- Визуальные эффекты при перетаскивании
- Логирование операций перестановки в историю

#### 6. data.js - Начальные данные
**Ответственность:**
- Предустановленные навыки, протоколы и состояния
- Инициализация приложения с примерами данных
- Базовая конфигурация

## Поток данных

### Инициализация
1. `app.js` запускает `init()`
2. Загружаются данные из `storage.js`
3. Если данных нет - загружаются из `data.js`
4. Инициализируются все модули (`ui.js`, `modals.js`, `dragdrop.js`)
5. Отрисовывается начальная страница

### Выполнение протокола
1. Пользователь кликает кнопку протокола
2. `storage.js` создает новую отметку (checkin)
3. Рассчитываются изменения для всех целевых навыков
4. Обновляется история и localStorage
5. `ui.js` перерисовывает интерфейс с новыми баллами

### Добавление/редактирование
1. `modals.js` открывает модальное окно
2. Пользователь заполняет форму
3. Данные валидируются и отправляются в `storage.js`
4. `ui.js` обновляет отображение
5. Показывается уведомление об успехе

## Особенности реализации

### Система ID
- Навыки и протоколы могут иметь как числовые, так и строковые ID
- Система сравнения поддерживает оба типа (`===` и `==`)
- При создании новых элементов используется `Date.now()` как ID

### Расчет баллов
- Каждый навык имеет `initialScore` (начальный балл)
- Текущий балл = начальный + сумма всех изменений из истории
- Минимальное значение = 0 (не может быть отрицательным)

### Поиск
- Работает по названиям, описаниям и hover-текстам
- Поиск регистронезависимый
- Фильтрует в реальном времени при вводе

### Отмена действий
- Каждое действие (выполнение протокола, перестановка) записывается в историю
- Можно отменить любое действие из истории
- При отмене drag & drop восстанавливается точная позиция

## Технические детали

### Хранение данных
Все данные хранятся в localStorage с ключами:
- `rpg_therapy_skills` - навыки
- `rpg_therapy_protocols` - протоколы  
- `rpg_therapy_states` - состояния
- `rpg_therapy_checkins` - история действий
- `rpg_therapy_skill_order` - порядок навыков
- `rpg_therapy_protocol_order` - порядок протоколов

### Обработка событий
- Используются современные `addEventListener` вместо `onclick`
- Для предотвращения конфликтов blur/click используется `mousedown` + `preventDefault`
- Debounce для поиска и автосохранения

### Стилизация
- CSS переменные для цветов темы
- Flexbox для расположения элементов
- Анимации через CSS transitions
- Адаптивность через media queries

## Расширение системы

Для добавления новых функций рекомендуется:

1. **Новые типы данных**: добавить в `storage.js` + методы CRUD
2. **Новые страницы**: расширить навигацию в `app.js` + методы в `ui.js`
3. **Новые модальные окна**: добавить HTML в `index.html` + логику в `modals.js`
4. **Новые вычисления**: расширить методы расчета в `storage.js`

Архитектура спроектирована модульно, каждый файл отвечает за свою область, что упрощает поддержку и развитие. 